NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# LMS Decode
LMS_DECODE_URL=http://192.168.1.38/api/lms-dev/v1/decode

# MSSQL Database (production / default)
MS_SQL_DB_SERVER=
MS_SQL_DB_NAME=
MS_SQL_DB_USER=
MS_SQL_DB_PWD=
# MS_SQL_DB_PORT=1433

# MSSQL Dev Database (used when devmode=true)
# Falls back to MS_SQL_DB_* values when these are not set.
MS_SQL_DEV_DB_SERVER=
# MS_SQL_DEV_DB_NAME=
# MS_SQL_DEV_DB_USER=
# MS_SQL_DEV_DB_PWD=
# MS_SQL_DEV_DB_PORT=1433

# SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=devlogger@lapilabs.co.id
DEV_EMAIL_RECIPIENTS=ITnotifLMS@lapilabs.co.id,michael@mikelabs.cloud
